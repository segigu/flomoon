# ============================================================================
# Flomoon Shared Environment Variables
# ============================================================================
# This file contains NON-SECRET configuration that can be committed to git
# For actual credentials, use .env.local (not committed to git)

# ============================================================================
# AI Mode Control (Phase 3: Hybrid Architecture)
# ============================================================================
# false = Direct API (local dev - fast, requires keys in .env.local)
# true  = Supabase Edge Functions (production - secure, no CORS)
# Default: false (local dev mode)
# REACT_APP_USE_EDGE_FUNCTIONS=false

# ============================================================================
# AI Configuration
# ============================================================================
# API keys should be set in .env.local for local development
# See .env.example for template
#
# REACT_APP_CLAUDE_API_KEY=sk-ant-...      (set in .env.local)
# REACT_APP_OPENAI_API_KEY=sk-...          (set in .env.local)
#
# NOTE: Direct API calls are for local development only
# Production will use Supabase Edge Functions (Phase 3)

# ============================================================================
# GitHub Pages
# ============================================================================
# PUBLIC_URL is now set in package.json homepage field
# PUBLIC_URL=https://segigu.github.io/flomoon
