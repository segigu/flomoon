# ============================================================================
# Flomoon Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to git (it's in .gitignore)

# ============================================================================
# Supabase Configuration (REQUIRED)
# ============================================================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api
REACT_APP_SUPABASE_URL=https://your-project-ref.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# AI Mode Control (Phase 3: Hybrid Architecture)
# ============================================================================
# false = Direct API (local dev - fast, requires keys below)
# true  = Supabase Edge Functions (production - secure, no CORS, uses Supabase Secrets)
# Default: false (local dev mode)
REACT_APP_USE_EDGE_FUNCTIONS=false

# ============================================================================
# AI Configuration (DIRECT API mode only - at least one provider required)
# ============================================================================

# Claude API (Primary AI provider)
# Get API key from: https://console.anthropic.com/
# Recommended: Use Claude Haiku 4.5 for best cost/performance ratio
REACT_APP_CLAUDE_API_KEY=sk-ant-your-key-here

# Claude Proxy (Optional - if using custom proxy)
# Leave empty to use direct Anthropic API
REACT_APP_CLAUDE_PROXY_URL=

# OpenAI API (Fallback provider)
# Get API key from: https://platform.openai.com/api-keys
# Used as fallback if Claude fails or is unavailable
REACT_APP_OPENAI_API_KEY=sk-your-openai-key-here

# OpenAI Proxy (Optional - if using custom proxy)
# Leave empty to use direct OpenAI API
REACT_APP_OPENAI_PROXY_URL=

# ============================================================================
# Notes
# ============================================================================
# 1. Supabase credentials are REQUIRED - app won't work without them
# 2. At least ONE AI provider (Claude OR OpenAI) must be configured (if using direct API mode)
# 3. Claude is the primary provider, OpenAI is automatic fallback
# 4. Proxy URLs are optional - use only if you have custom proxy setup
# 5. After changing .env.local, restart dev server (npm start)
# 6. REACT_APP_USE_EDGE_FUNCTIONS=false: Direct API (local dev - keys visible in bundle)
# 7. REACT_APP_USE_EDGE_FUNCTIONS=true: Supabase Edge Functions (production - secure, no CORS)
# 8. For production, set USE_EDGE_FUNCTIONS=true and configure Supabase Secrets (see Phase 3 docs)
